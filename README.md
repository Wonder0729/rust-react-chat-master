# Rust React Chat Master

This is chat app using `Rust(actix-web)` as back-end and `React` as front-end.

## Windows 10/11 Setup.
```
git clone https://github.com/Wonder0729/rust-react-chat-master.git
cd rust-react-chat-master
cargo run
cd ui
npm install
npm start
```
